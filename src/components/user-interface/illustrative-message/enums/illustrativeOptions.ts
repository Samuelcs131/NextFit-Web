/* eslint-disable no-unused-vars */
export enum illustrativeOptions {
  file = 'file',
  folder = 'folder',
  calendar = 'calendar',
  search = 'search',
  notebook = 'notebook',
  dashboard = 'dashboard',
  empty = 'empty',
}
